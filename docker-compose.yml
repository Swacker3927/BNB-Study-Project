services:
  service:
    build:
      context: .
      dockerfile: Dockerfile
    image: bnb_server
    container_name: bnb_server
    restart: always
    ports:
      - "18000:18000" # 외부포트 : 내부 앱실행 포트
    volumes:
      - /home/ubuntu/apps/uploadData:/upload
